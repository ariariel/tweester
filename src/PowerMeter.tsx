import React from 'react'

type PowerMeterProps = {
  active: boolean,
  onPowerReading: Function
}

const PowerMeter = ({ active } : PowerMeterProps) => {
  return (
    <></>
  )
}

export default PowerMeter;
